// Task Constructor
function Task(title, bodyText, targetDate, targetTime, id) {
  this.title = title;
  this.bodyText = bodyText;
  this.targetDate = targetDate;
  this.targetTime = targetTime;
  this.id = id;
}
